# Backlog

## High Priority

| Item | Tag | Difficulty | Notes |
|------|-----|-----------|-------|
| CUDA support (full hardware exercise) | feature | M–L | Torch backend is wired for CUDA end-to-end but completely untested on real hardware — the CI lane is macOS, so every CUDA path has only been validated by compile. **Machinery in place**: device taxonomy parameterises `TorchDev` over `TorchHwDev = TCpu \| TMps \| TCuda Nat` (`packages/idris-ml/src/Device/Torch.idr`, from the 2026-05-19 device-taxonomy refactor); `BuildConfig.idr` accepts `TORCH_DEVICE=cuda` and emits `TorchDev (TCuda 0)` via the Makefile's `BUILDCONFIG_IDR` rule; `Tensor [...] (TorchDev (TCuda 0))` compiles; `toDevice` + the C-side `tensor_to_device(h, "cuda:0")` FFI both exist; the availability-gating epic (compile-time `Linked` + runtime EAFP presence gate) shipped 2026-05-21 (see `docs/develop/device-availability-gating.md`). **Required when a CUDA box is available**: (1) run the smoke test — `scripts/test_cuda_colab.sh` is staged for this; see `docs/develop/cuda-testing.md` for the procedure. (2) Perf-sweep — `bash scripts/perf-sweep.sh --cells torch-cuda` on the canonical 6 examples; validate convergence + ratio vs PyTorch ref under `--device cuda` (smoke targets match the 2026-05-19 sweep set). (3) Multi-GPU `TCuda n` enumeration — `builtinDevices` (`HwDevices.idr`) lists only `TCuda 0` today; enumerate `TCuda 0..k-1` bounded by a native `cuda_device_count` so `availableDevices builtinDevices` surfaces every CUDA GPU (folded in from the now-merged hardware-availability-gating row, which is otherwise complete). (4) **BF16 / F16 tensor-core perf verification** (folded in from the now-deleted Medium row "BF16 / F16 GPU-fast paths — CUDA tensor cores" on 2026-06-06): All Compatible instances `(TorchDev (TCuda n)) BF16/F16` already exist (`Device/Torch.idr:668,674`); torch backend dispatch is uniform across `TCpu/TMps/TCuda` (`backend_torch/training/dtype_dispatch.cpp:53-103` — no per-device gates); autocast + GradScaler shipped 2026-06-01 (separate row, closed). The only missing piece is *running it on CUDA hardware*: re-run the perf-sweep above (step 2) under `MLX_DTYPE=BF16` and `MLX_DTYPE=F16` and the torch equivalent; expect BF16 / F16 wall_ms < F32 wall_ms on the larger examples (HfLlama-1B, transformer-translation, gpt-shakespeare). The "BF16 runtime genuinely dominates F32" hypothesis was measured *not* to hold on libtorch's MPS path (perf-changes.md 2026-05-31; within noise) nor on mlx-gpu (+62% on HfLlama, pending the row "Localize mlx-gpu BF16 vs F32 wall gap on HfLlama-1B inference"); the CUDA tensor-core path is the last regime where the hypothesis is plausibly true. The tape lingua-franca BF16/F16 storage (2026-05-23 tape-dtype-parity) stays as the correctness oracle. Cross-references: `packages/idris-ml/src/Device/Torch.idr` (TorchHwDev + `Compatible (TorchDev (TCuda n)) BF16/F16` instances at lines 668, 674), `Makefile` BUILDCONFIG_IDR rule (TORCH_DEVICE=cuda branch), `docs/develop/cuda-testing.md`, `docs/develop/device-availability-gating.md`, `scripts/test_cuda_colab.sh`, `docs/develop/perf-changes.md` 2026-05-31 (MPS BF16 within-noise + mlx-Metal +62% measurements), commits `e2ad295` mlx-BF16 + `9856771` mlx-F16. |
| Publish to package managers | infra | S–M | Make idris-ml installable via standard Idris 2 package channels. Investigate: (1) [pack](https://github.com/stefan-hoeck/idris2-pack) — the main Idris 2 package manager, needs a `pack.toml`; (2) add to the [pack-db](https://github.com/stefan-hoeck/idris2-pack-db) collection. Note: the C backend (`libidrisml`) needs special handling — pack doesn't natively support native library deps. May need a `postinstall` script or `make backend` instruction. Also consider publishing to GitHub Releases with prebuilt dylibs for common platforms |
| User-facing documentation redesign / overhaul | docs | M–L | The repo has accumulated developer-facing docs (`CLAUDE.md`, `docs/develop/*.md`, `design-decisions.md`, gotchas, perf-changes, reference-alignment, coverage-policy, etc.) but the user-facing story — README front-page, tutorial index, Jupyter notebooks, "how do I do X" guides — has not kept pace with the feature surface. **Audience split that the redesign needs to make explicit**: (a) the **library user** training their own models (the README + tutorial notebooks should serve this person), (b) the **researcher** building on idris-ml's typed-tensor / autograd machinery (needs a guided tour of the architecture: Device / DType / Tensor / Layer / Network / Train), (c) the **contributor** extending backends or adding ops (already well-served by `docs/develop/`). Concrete user-facing gaps that have piled up: **(1) Checkpointing** — wired into 7 examples (gpt, transformer, ntm-copy, dnc-copy, rnn, lstm, gru) with `--checkpoint-dir` / `--resume` / `--checkpoint-every` (closed 2026-05-21); needs a user guide covering the flag set, the three on-disk artifacts, the `trainer_state.json` sidecar, return-best semantics. A Jupyter cell demonstrating "kill the kernel mid-train, reattach, continue" is the headline use case (cross-ref `Checkpoint.idr` / `Train.idr` `withCheckpoint` / `scripts/test-checkpoint-resume.sh`). **(2) Device + dtype selection** — `BACKEND=tape,torch,mlx` + `TORCH_DEVICE` / `MLX_DEVICE` + the F32/F64 cell table is in CLAUDE.md but a user walking up cold has no guide; the BuildConfig generator + the `(ExampleDevice, ExampleDType)` pattern need a short narrative. **(3) The Tensor / Layer / Network / Train story** — the typed-shape value prop is the headline feature and has no narrative doc that a newcomer can read top-to-bottom. **(4) HuggingFace checkpoint loading** — once that row lands, a tutorial loading tiny-llama is the obvious showcase. **(5) Jupyter notebooks** — `packages/jupyter/` carries some examples but no curated tutorial sequence (forward pass → autograd → training loop → checkpointing → custom layer); design a 5-7 notebook progression. **(6) README** — feature list, quick-start, "why dependent types for ML" pitch, link map. **Out of scope here**: `docs/develop/` developer docs (those are healthy). Deliverable shape: a `docs/users/` (or `guides/`) tree + the notebook sequence + a README rewrite, gated by a clear contents-of-each-page sketch up front so the audience split is enforced. |
| Support fine-tuning workflows on HF-loaded models | feature | M–L | The HF adapter packages (`HfBert` / `HfGpt2` / `HfLlama` / `HfBitNet`) only exercise the **inference** path today — load checkpoint via `loadModel`, forward, decode. The fine-tuning story isn't yet wired: (1) **Param flagging** — `loadModel` currently registers every parameter into the optimizer registry; for LoRA / partial-freeze workflows we need a way to mark a subset of parameters as frozen vs trainable (PyTorch's `requires_grad=False` per-param + optimizer param-group filtering). The `paramId`-prefix filtering in `nativeAdamGroup` is the closest existing primitive, but it filters at *optimizer* level, not at autograd-tape level — frozen params still accumulate grads, then get zeroed at step time. **(2) Adapter layers** — LoRA-style low-rank adapters (`W + α·B·A` where B, A are the trainable low-rank pair) need a small per-target-linear wrapper module. PyTorch's `peft` library is the reference. **(3) Mixed-precision training on these models** — already partially covered by other rows (mlx-Metal BF16 landed 2026-05-31; torch CUDA tensor-core path gated on the CUDA-support row), but fine-tuning is the canonical workload where bf16/fp16 + bf16/fp32 accumulator pattern matters. **(4) Training-loop integration** — `runTraining` / `runTrainingIO` work today for from-scratch examples; HF-loaded models need a worked example showing the full loop (load → freeze → adapter inject → train → checkpoint → eval-merge). **(5) Dataset adapters** — HF Datasets is the canonical companion; a minimal `Dataset` interface paired with the Tokenizer machinery would let fine-tuning examples target a real corpus rather than synthetic data. Worked example to land: fine-tune `HfBert` on a small classification task (SST-2 / GLUE-subset), validate the loss curve matches PyTorch's `Trainer` within reasonable tolerance, time on torch-mps + mlx-gpu. **Why high priority**: the inference-only adapter package is half the story — fine-tuning is what a library user actually wants to do with their typed-tensor Idris ML library after the "load Llama-3.2-1B and decode" demo. **Dependencies**: per-param freeze gate (autograd-tape-level, not optimizer-level), small LoRA-style adapter module, worked example + cross-language convergence gate. **Out of scope under this row**: distributed fine-tuning, DPO / RLHF / preference-pair training (separate downstream rows), full-parameter fine-tuning of 1B-class models on 16 GB VMs (memory-physics carve-out). |
| Critique the main library API + record findings | research | M | Audit the user-facing surface of `packages/idris-ml/` (what a `import Tensor` / `import Layer` user sees) for naming clarity, abstraction joints, idiom-vs-ergonomics, and divergence from PyTorch / JAX where the divergence isn't load-bearing. Deliverable: `docs/develop/api-critique.md` with per-surface verdicts ("rename X to Y", "merge X and Y", "split X", "rationale stands"), each backed by call-site counts + a tradeoff sketch. Concrete surfaces to assess: **(1) the Tensor smart-constructor wall** — `tinput1d` / `tinputN` / `bulkToTensor` / `bulkToTensor2d` / per-rank explicit `tvecToVector` / `treadVec` etc. — does this need a unified `fromArray` / `toArray` API instead of per-rank explicit helpers? **(2) `Network` + `OutputLayer` chain composition** vs PyTorch's `nn.Sequential` + `nn.Module` — the existing "Reconsider layer / module naming" row covers the rename axis; this row covers whether the COMPOSITION ABSTRACTION itself is the right one. **(3) Loss-as-function vs loss-as-class** — `crossEntropy : Tensor -> Tensor -> IO Tensor` vs PyTorch's dual `nn.CrossEntropyLoss` (stateful) + `F.cross_entropy` (functional); pick one. **(4) Optimizer API** — `nativeAdamGlobalClip` / `nativeAdamGroup` / `nativeAdamW` / `nativeSgd` / `nativeRmsprop` — proliferation of "native" prefixed variants; can these collapse to one `optimizer` builder with config record? **(5) Training driver** — `runTraining` / `runTrainingIO` / `epochVar` / `epochVarTensor` / `epochVarTensorBatch` / `epochRecurrentVar` / `epochTwoPhaseVar` — six epoch-step variants is a lot; is the "mode" really six things, or is it one polymorphic function over a `DataShape` indexed type? **(6) Checkpoint integration** — `withCheckpoint` / `fileCheckpoint` / `CheckpointPolicy` — recently landed but unclear if the API shape is right for typical users vs power-users. **(7) Sampler / DataLoader interplay** — `DataLoader` + `Sampler` + `DataPoint` + `TensorDataPoint` is a 4-type cluster; PyTorch's equivalent is `DataLoader` + `Dataset` + `Sampler` (3); investigate which carves at joints. **(8) HF adapter scaffolding** — `HfBert.idr` / `HfGpt2.idr` / `HfLlama.idr` / `HfBitNet.idr` proliferation, separate from the "DRY-up the `idris-transformers` adapter package" row — there the question is whether to share code; here it's whether the per-arch ipkg + module split is the right user-facing shape. **(9) Implicit args + interface constraints on every smart constructor** — `{0 d : Device} -> UserDeviceTraining d => {n : Nat} -> Tensor [n] d dt g -> IO ...` is the canonical signature; the same prefix appears on dozens of functions. Is there a way to elide some via type-inference or interface aliases? **Approach**: audit ~half-day per surface (8 surfaces × 0.5d = ~4d total); compile into one critique doc; file each verdict that scores "needs change" as its own follow-up row. **Why high priority**: the existing fine-tuning + user-doc + naming-rework rows all assume the current API is the right shape to document/extend; an API critique should land FIRST so we're not writing docs for something that ends up renamed three months later. Cross-references: the existing related rows (29 "Reconsider build/link backend model", 31 "Reconsider layer / module naming", 32 "Audit PyTorch design decisions for ideas to borrow", 39 "DRY-up idris-transformers"), the related backend/device DRY row added today. Deliberately separate from these — those are about IMPLEMENTATION DRY or SPECIFIC SURFACE RENAMES; this row is the higher-level "is the joint set right?" question over the whole user-facing API. |
| Generate Idris instance method lines from the FFI manifest | refactor | M | Residual of the larger "DRY-up + conceptual rework of the backend / device surface" row, now narrowed. Most of that row's substance landed: drift detection (`scripts/check-executor-method-drift.py` + CI wire, design-decisions.md "Sub-slice split rather than monolith collapse"), the Machine/Hardware/Backend/Executor open-kind reshape (design-decisions.md "2026-06-06: Machine / Hardware / Backend / Executor open-kind reshape"), the `UserExecutorTraining` → 6-sub-slice split + `UserExecutorInference` aggregate (commit `ceda8f30`), and the `Compatible` × `RuntimeDType` orthogonality decision (design-decisions.md "`Compatible` and `RuntimeDType` are orthogonal"). What remains is the mechanical half of pain point (2) "every new primitive needs N+1 sites edited": the `%foreign` Scheme wrappers and `rename_*.h` files are already generated (`scripts/lifecycle/ffi-convert-to-scheme.py` + `scripts/gen-rename-headers.py`), but the ~168 per-backend Idris instance method lines (`primFoo = prim__fooBackend` for Tape/Torch direct; `primFoo a b = prim__fooMlxStreamed a b (streamTag s)` for Mlx streamed) are still hand-written and drift-prone. Plan: extend the manifest entries in `scripts/lifecycle/ffi_manifest.py` with `idris_method` + `slice` + `flavor` (Direct / Streamed / Bespoke) fields; write `scripts/lifecycle/gen-executor-instances.py` (+ `--check` mode) that emits the method lines into `-- BEGIN GENERATED [<slice>] / -- END GENERATED` marker pairs inside each instance `where` block in `Executor/{Tape,Torch,Mlx}.idr`. Bespoke methods (~8 total: Torch's `primCreateScalar` / `primCreate` device-migration wrappers, `primIntraMigrate`; Mlx's BitLinear-fwd variants + `absmeanPerRow2d`) stay below the END marker as hand-written overrides. Extend `check-executor-method-drift.py` (or sibling) to gate manifest ↔ instance-block alignment in CI. Reduces per-new-op manual sites from ~7 to 4 (C kernel × 3 + `backend.h` + manifest entry + interface method declaration). Cross-refs: `packages/idris-ml/src/Executor/{Tape,Torch,Mlx}.idr` (instance blocks), `scripts/lifecycle/ffi_manifest.py`, `scripts/check-executor-method-drift.py`. Sister directions from the original row that **will not be pursued** — design-decisions.md captures the rationale: (a) monolith collapse (rejected; sub-slice split serves real use cases), (b) single Backend record (partially landed via the `Preset` typeclass + open kinds, residual not pursued), (d) `RuntimeDType` + `Compatible` unification (rejected as conflating orthogonal axes), pain point (5) per-(executor, dtype) `Compatible` explicitness (left as-is; carve-outs are intentional and have hardware reasons). |
| Investigate Idris-2 JIT / JAX backend (separate question marks) | research | M–L | Two orthogonal investigations that came up while chasing the GPU-friendly story: (1) **Idris-2 JIT** — does the Chez Scheme runtime have any meaningful JIT pass for hot loops, or are we paying interpretive overhead per FFI call? The 2026-05-14 Phase 3 measurements show ~8 s/ep of Idris-side wallclock dominates every backend at GptLarge, and the GPU is ~570 ms/ep slower than CPU on that side alone (separate row below). If Chez has a `with-jit` mode or a way to compile hot paths, that's a big lever. (2) **JAX backend** — JAX's `jit` + XLA fuser is the most mature path for `compile-the-whole-step` semantics. A JAX backend (via XLA's HLO or via the JAX C++ API) would slot into the `UserDeviceCore` interface alongside tape/torch/mlx. Cost: depends on whether JAX exposes a usable C++ API or whether we'd need a Python embedding (significant overhead per FFI). Decision points: (a) is JAX's C++ API stable enough to depend on? (b) does XLA's HLO emitter solve the per-param-update-graph problem cleanly? (c) does adding a fourth backend create maintenance overhead disproportionate to the wins? Investigation deliverable: ~half-day spike per question, with a written-up recommendation appended to `docs/develop/`. Treat as exploratory; outcome may be "no" on either or both |

## Medium Priority

| Item | Tag | Difficulty | Notes |
|------|-----|-----------|-------|
| Idris2 linter | infra | M | The codebase has accumulated lint-like rules enforced ad-hoc via grep-based shell scripts (`scripts/check-executor-method-drift.py`, `scripts/check-ffi-wrap-template.sh`, `scripts/check-rename-headers.sh`, `scripts/check-gradmode-*.sh`, etc.) and CI gates. A proper Idris 2 linter would catch a larger class of issues uniformly: unused imports, shadowed binders, missing `paramId` on layer constructors (the silent-gradient-leak class), wrong `Compatible` constraint shape, `let _ = ffiCall` patterns dropped by the codegen (per `feedback_pure_typed_ffi_reorders.md`), bare `Tensor [n * m] d` patterns that hang the elaborator (per `feedback_idris2_tvar_nat_mult.md`), `total` used as a variable name (compiler keyword), forgotten `%foreign` wrapping templates, etc. Investigate: (1) is there an extant community Idris 2 linter (idris-bifocals, idris-tools, the Idris 2 IDE protocol's hover/check messages)? If yes — wire it into `make check` and CI. (2) If no — sketch a minimal one. Idris 2's `--check` already runs the elaborator; a linter could be a separate parsing pass over `.idr` source that fires custom warnings the elaborator doesn't model. Pure-Python sufficient for grep-tier rules (codebase already has 8+ such scripts); a real linter would parse Idris 2's surface syntax (the existing `scripts/check-executor-method-drift.py` already does a small fraction of this). Deliverable: either a `make lint` target wrapping a community tool, or a contributor-facing `scripts/lint.py` covering the high-value rules above, plus CI wire. Cross-refs: `scripts/check-*.sh`, `scripts/check-*.py`, `feedback_*.md` entries in `~/.claude/projects/-Users-admin-code-idris-ml/memory/` that codify rules currently enforced by review, not tooling. |
| HfLlama torch-mps decode wall: gap to PyTorch Python beyond op-count and rank-3 broadcast | perf | M–L | **Re-framed 2026-05-31 (closes the original "Investigate rank-3 broadcast per-op cost" formulation — see CHANGELOG)**. The original premise (~10-26 ms per rank-3 broadcast mul in our wrapper) was refuted by a four-layer microbench ladder (`bench_rank3_broadcast.cpp` direct libtorch, `bench_rank3_broadcast_wrapped.cpp` C-wrapper, `RankBroadcastBench.idr` Idris-level, `time_rank3_broadcast.py` PyTorch Python). All four lanes converge on **15-26 µs/op** for `mul([6,32,32], [6,1,32])`. The 10-26 ms figure was an arithmetic artifact: `runGenerate wall (~317 s) / total ops (~21K) ≈ 15 ms/op` collapses to "per-op cost" only if every op is the same size — but after `c09d374` (all-heads RoPE) each remaining op carries 32× the data, so total compute is constant, op count drops 7×, and `wall/op_count` *appears* to rise. **The real gap to PyTorch Python (5:17 vs 2 s on the same MPS device)** is unrelated to the rank-3 broadcast cost. Untested follow-ups: (a) **op-count reduction via more fusion** — the existing TODO Medium #42 "Match PyTorch's catalogue of fused ops" row covers this; SDPA landed but RMSNorm / SwiGLU / fused-linear-activation chains remain. (b) **per-op cost on LARGER shapes** — write a matmul-shaped microbench (Llama attention QKᵀ is `[seq=N, headDim=64] @ [headDim=64, seq=N]`; FFN's up-project is `[seq, hidden=2048] @ [hidden=2048, intermediate=8192]`) to confirm matmul per-op cost matches libtorch direct. (c) **per-`ioRerun` cost** — each `applyRopeAllHeads`-style smart constructor is wrapped in `ioRerun (\_ => let ... in MkTensor ...)`; if there's a per-`ioRerun` allocation/closure cost and HfLlama has thousands of layer-level calls, that's the real overhead. Instrument once and measure. **Why this row stays open**: PyTorch Python on the same MPS hardware does this workload 158× faster; closing that is the headline HfLlama perf objective. **Why M–L not L**: with the rank-3 broadcast formally ruled out, the search space is narrower. Most likely candidates are op-count (already on row #42) and op-mix at the matmul level (one more microbench to file). Cross-references: `bench_rank3_broadcast{.cpp,_wrapped.cpp}`, `RankBroadcastBench.idr`, `time_rank3_broadcast.py`, perf-changes 2026-05-31 entries Commit 1 / 1b / 1c, the closing row in `CHANGELOG.md`. |
| Cross-backend auto device switch | feature | L | **Reframed 2026-06-06** from "MLX auto device selection" once a Hardware/Executor audit confirmed MLX has no built-in scheduler — it exposes `mx::set_default_device`, `mx::Stream`, `mx::StreamContext`, but no per-op auto-router. The work is identical for torch CPU↔MPS↔CUDA. Implementation surface: a per-op heuristic at the Idris typeclass level (new `MlxAutoExecutor` / `TorchAutoExecutor` wrappers that branch `streamTag` / device pick based on shape) or a C-side branch (each `_streamed` FFI inspects shape and routes). Per `project_mlx_gpu_environment.md`, MLX GPU is 3–12× *slower* than CPU stream at idris-ml's example sizes (kernel-launch wall) — so a heuristic today mostly says "always CPU." Defer until LLM-scale workloads make GPU consistently faster (matmul `m*n*k > T` where T is calibrated from `MatmulBench`). Original design space (typeclass-level, C-side, explicit `withAutoDevice`) preserved in commit history. |
| Multi-machine flows (distributed execution) | feature | L | **Added 2026-06-06** when the `Machine` open kind landed (Phase A.3 of the Machine/Hardware/Backend/Executor rework). With `Machine = Type` and `Provides m h` typeclass in place, distributed-execution / multi-host flows become representable in the type system. Sketch: a `Tensor dims (0 d : Executor) (0 dt : DType) (0 g : GradMode)` pins one Executor; a multi-host program would model `Tensor dims (0 m : Machine) ...` with a higher-level Executor that's "this Machine's local Executor" or "the remote-machine Executor reachable via RPC". Out of scope for years — distributed Idris-ml runs are not a near-term goal — but worth marking so the `Machine` kind isn't a dead-end. Concrete first step would be: (1) define `LocalMachine : Machine` and `RemoteMachine : (host : String) -> Machine` tags; (2) write a `RemoteExecutor h` wrapping `(host, target-executor)` with RPC-shipping `UserExecutorCore` methods; (3) one example showing M-series host orchestrating CUDA workstation. |
| ~~Extend op-level `IsFloating` gating through the layer/network stack~~ — **decided against** | feature | — | Op-level dtype-kind gates landed 2026-05-22 (commit `03a2c41`): `IsFloating`/`IsIntegral` in `DType.Core`, with the loss fns + gradient surface (`runBackward`/`nativeTrainStep`) gated on `IsFloating dt`. Extending that to the **activations** (`ttanh`/`tsoftmax1d`/…) was considered and **declined** (2026-05-22): **(1) selective gating is impossible** — `LayerLike` (`Layer/Core.idr`) is parameterized only by the layer type `l`; `dt` is *method-quantified* on `applyVar`, so a `dt` constraint must sit on the interface method (= every layer instance) or nowhere — there is no instance-head trick to gate only the float-needing layers. **(2) The "non-float" layers aren't non-float** — a layer's `dt` is the *activation* dtype, which is float in every real network (Embedding's token "indices" ride in `dt` as floats-representing-ints, same as argsort). **(3) Low value** — interface-gating would only forbid *forward* passes on a non-float activation tensor (a case nobody hits); the meaningful invariant — no backprop/loss on a non-float tensor — is **already** enforced at the gradient/loss surface. Revive only if a concrete need appears. The structural change that *would* matter (separating param-dtype from activation-dtype, e.g. for ternary/quantized weights) is captured on the BitNet row. |
| **Idris-side per-op overhead (cross-backend wall bottleneck) — partial** | perf | M–L | **Diagnosed and first fix landed 2026-05-14** (perf-changes "Where the 9000 ms/ep GptLarge wall actually goes" entry). Originally framed as the mlx wall bottleneck; the tape-side analysis on the same (now-retired) GptLarge workload found the same root cause via a different measurement — ~0.6 ms/tape-entry × 293 entries × 32-sample batch ≈ 5.6 s/ep matched tape's 5.8 s "C-time" headline almost exactly, confirming the headline was misattributed inter-op overhead and not C compute. Both backends share the same Idris-side floor; row was merged from the former "Fix tape per-tape-entry Idris/Chez overhead" row on 2026-05-17. **Mlx diagnostic**: on retired GptLarge (CPU or GPU, indistinguishable in this VM), wall was 8600-9000 ms/ep with C-total ≤140 ms/ep — i.e. ~8870 ms/ep of Idris VM time between FFI calls. Per-FFI-call wall via `/tmp/bench_per_op` = 0.46 µs; tape_append counter = 1136 grad-tracked ops/epoch; total FFI wall = 0.5 ms/ep = **0.006% of wall**. Per-op Idris cost = 8600 / 1136 = **7.6 ms per tensor op**. This kills any "compile-the-whole-step / eliminate FFI" plan — there's nothing meaningful to save on the FFI side. **The lever is Idris-side code.** Suspects (need targeted measurement to localise): (1) existential `AnyLayer` dispatch in `Network` chain — every forward walks the chain via `~~>`, one indirection per layer; (2) constraint dictionary resolution at every op site (`UserDeviceCore d`, `LayerLike d`) if Idris isn't fully inlining them — see `feedback_typeclass_zero_arg_method_eval.md` for the related typeclass-method gotcha; (3) `Tensor` record packing/unpacking; (4) `Vect` operations in shape arithmetic; (5) per-op Idris closures inside layer methods; (6) **Chez foreign-procedure dispatch cost** — *was promoted to a High-priority row 2026-05-27 after a sample-profile of HfLlamaInference confirmed `S_foreign_entry → lookup → dlsym` accounted for ~100% of wall time on Llama-3.2-1B inference. Fix shipped same day (commit `2385e3f`): lazy-cache `foreign-procedure` per FFI via `top-level-bound?` so subsequent calls do a top-level-value lookup instead of constructing a fresh foreign-procedure (which re-dlsym'd). High-priority row closed 2026-06-03; see `CHANGELOG.md` + `docs/develop/perf-changes.md` 2026-06-03 entry. The remaining Idris-side per-op overhead is the OTHER suspects in this list — most likely (7) GC pressure or (5) per-op closure / typeclass-dispatch.*; (7) **Idris allocation pressure triggering Chez GC during forward** (tape-side suspect — observable via `IDRIS2_CG_ARGS` GC stats during a forward). Investigation plan: instrument `forwardVar` + `applyVar` with Idris-side `getTime`, isolate which method+layer-type combinations are slow, then attack hotspots; for the Chez-side suspects, `IDRIS2_CG_ARGS` GC stats during a forward and a `dtrace`/`Instruments` sample. Worth doing as small probes (1-2 days each) rather than as a multi-week refactor up front. Since GptLarge is retired, the live target workloads are whichever current examples have the highest tape-entry count + bench-compare ratio (NTM/DNC/Transformer typically) — pick one with a clean ratio-vs-PyTorch gap as the measurement vehicle. Half-quartering 7.6 ms → 3.8 ms = ~50% wall reduction on those workloads. Orthogonal fix candidate carried over from the tape row: refactor the per-op profile attribution to record kernel-internal time, so misattribution stops corrupting the signal regardless of the underlying cause |
| Port batched policy forward to remaining RL examples | perf | M–L | The batched policy forward landed on `Reinforce.idr` only (2026-05-12; mlx −37% / torch −16% / tape noise-bound on the 100-epoch comparison; bit-identical convergence). The other RL examples don't fit a "drop-in port" because the batched form changes training dynamics: **a2c, ppo** carry a single env state across epochs at fixed `RolloutLen` — N parallel envs means N× more on-policy data per gradient step, which is a semantic change requiring paired-side `torch_ref/scripts/{a2c,ppo}.py` updates + hyperparameter retuning per the alignment policy. **sac, dqn, mountain-car, mountain-car-cont** are off-policy with replay buffers; batching the rollout changes buffer fill rate per gradient step (same dynamics-change concern). Each example is its own multi-stage TDD effort: (1) parity test on the new batched rollout vs sequential at N=1 / N=2; (2) batched implementation; (3) paired `torch_ref/scripts/*.py` update for N parallel envs; (4) hyperparameter retune at the new batch size; (5) convergence sweep on both sides; (6) ms/epoch measurement. Files: `packages/idris-ml-examples/src/Example/{A2c,Ppo,Sac,Dqn,MountainCar,MountainCarCont}.idr` + matching `torch_ref/scripts/*.py`. Per-example pattern is captured in the batched-policy-forward work on `Reinforce.idr` (see `perf-changes.md` 2026-05-12 entry and `Test.Reinforce` for the TDD shape). Do them on-demand when an example's perf gap or training-time becomes a pinch point |
| **Match PyTorch's catalogue of fused ops (epic)** | perf | L | Multi-item, measurement-driven push to bring idris-ml's fused-op surface in line with PyTorch's. Standard for what's in scope is the **precedent test**: if PyTorch ships a fused equivalent (`aten/src/ATen/native/`, `torch.nn`, `torch.nn.functional`), we can have one too — see `feedback_pytorch_precedent_test.md`. Motivation: the "absolute speed" gap on DNC-class workloads (9.8×-14.2× vs PyTorch ref per the 2026-05-09 sweep) is per-op-count, not kernel quality — one PyTorch `nn.LSTMCell` is one C kernel and one autograd-graph node, ours expands to ~10 prim calls and 10 tape entries. Closing that requires fusing where PyTorch fuses, plus where PyTorch *would* fuse but doesn't on CPU (their CPU LSTM/GRU cell is unfused — we already win there via `OP_LSTM_GATES`). Existing fused ops worth keeping current: `OP_LSTM_GATES`/`OP_LSTM_GATES_CELL`, `OP_LAYER_NORM_2D`, `OP_SOFTMAX_2D`/`OP_SOFTMAX_3D`, `tensor_lstm_cell`/`tensor_gru_cell`/`tensor_cosine_similarity`. Concrete pending items, by demand: **(1)** `tensor_softmax_cross_entropy_with_logits` (PyTorch: `F.cross_entropy`) — replace today's no-grad `tensor_cross_entropy` (`backend_tape.c:1615`) + decomposed log_softmax + nll path with one fused fwd+bwd op (`grad = (softmax − target)/N`); ~150 LOC, lands one tape node instead of 3-4 per classification step. **(2)** `tensor_scaled_dot_product_attention` (PyTorch: `F.scaled_dot_product_attention` + the CPU FlashAttention kernel) — reference impl in `aten/src/ATen/native/cpu/FlashAttentionKernel.cpp`; ~600 LOC; defer until a transformer-heavy workload justifies. **(3)** ~~`tensor_rms_norm` (PyTorch: `torch.nn.RMSNorm`) — standard in modern LLM stacks; ~100 LOC; defer until an LLM-style example lands.~~ **SHIPPED** 2026-06-03 (`416c011`); see perf-changes.md. **(4)** `tensor_group_norm` (PyTorch: `nn.GroupNorm`) — small-batch image models. **(5)** fused activation+linear (`tensor_silu_linear`, `tensor_gelu_linear`) — PyTorch ships these via `torch._foreach` and inductor patterns; small, bundle with vec-helper work if measurement surfaces them. Out of scope (fails the precedent test): example-specific fusions like `ntm_cosine_softmax_mul` or `dnc_link_matrix_update`. Approach is **measurement-driven**: don't preemptively port every PyTorch fused op — pick one when a current example's profile shows the corresponding decomposed chain dominating. The `pytorch-survey.md` doc has the full surface inventory; the `feedback_pytorch_precedent_test.md` memory has the rule. Cross-reference: items (1) and (2) of the pytorch-survey's recommended-action list are scoped under this epic |
| Thread a seedable Rng through `idris-gym` `Env.reset` | feature | M | Today `Env.reset : state` is deterministic — each env has a hardcoded init (e.g. `Gym.ClassicControl.Pendulum.reset = MkP Pi 0.0`, the hangs-down worst-case). Canonical Gymnasium randomizes init within a per-env range (Pendulum: `theta ∈ [-π, π], theta_dot ∈ [-1, 1]`; CartPole: each state component `~ U(-0.05, 0.05)`; etc.). The gymnasium-migration row pins gymnasium's reset to match Idris in the torch_ref scripts, but the principled direction is opposite — randomize idris-gym to match canonical. Required interface change: `Env state action obs` interface's `reset` becomes `reset : Rng -> (Rng, state)` (or threads `Rng` via `Gym.Rng`'s existing SplitMix64 generator). Touches every env instance (CartPole, MountainCar, MountainCarContinuous, Pendulum, Acrobot, FrozenLake, CliffWalking, Taxi, Blackjack), `Gym.Vector` for sync vector envs, `Gym.Wrapper.{TimeLimit, Record, Normalize}` wrappers. Per-env reset distribution must match Gymnasium's reference; document any deliberate divergences in `reference-alignment.md`. After this lands, the torch_ref pins (e.g. `env.unwrapped.state = [π, 0.0]`) can be dropped and both sides will randomize identically. Independent: not blocked by the gymnasium-migration row, but unblocks dropping the pins it adds. |
| Survey Glaive Research for usable ideas | research | S | [glaive-research.org](https://glaive-research.org/) is a nonprofit applying category theory to AI verification, explicitly publishing in Idris with dependent types. Overlap with idris-ml is substantial — shape-indexed tensors, type-safe automatic differentiation, categorical composition — and they may have working formalisms we can borrow. Review their Q4 2025 report and the "Building a Neural Network from First Principles using Free Categories and Para(Optic)" post, extract any directly adoptable ideas (free-category composition for layer chains, Para/Optic view of backprop, applicative-functor generalized transformers, categorical autodiff semantics), and file follow-up tickets for the ones worth implementing. Deliverable: a short `docs/develop/glaive-survey.md` with per-idea "adopt / borrow interface / inspiration only / out of scope" verdicts. **Blocks the "Reconsider layer / module naming" Medium row** — the verdict on Para/Optic adoption will dictate whether the new naming scheme aligns to PyTorch's `nn.Module`, Glaive's `Para`, or pure CT `Morphism`. |
| Privacy-preserving ML (PPML) | feature | M–L | Examples and layers for differential privacy (DP-SGD, gradient clipping + noise), secure aggregation, and homomorphic encryption (HE-friendly activations). Inspired by OpenMined/PySyft. Idris 2's type system could enforce privacy budgets at compile time (e.g., epsilon-tracking via phantom types). Start with DP-SGD example (clip per-sample gradients + Gaussian noise in optimizer), then HE-compatible polynomial activations |
| Static graph optimizations | perf | L–XL | Compile-time operator fusion, memory planning via dependent types. Idris 2's type system knows tensor shapes at compile time — could fuse sequences like matmul+bias+relu into single kernels, plan memory reuse for same-shape intermediates, and eliminate dead computations. See `docs/static-vs-dynamic-graphs.md` |
| TensorBoard-style file sink for debugging | feature | M | Per-layer / per-epoch tensor outputs serialized to disk (e.g. `.npy` or a custom binary format) for inspection from external tooling — Python notebooks, TensorBoard, etc. Closer to how PyTorch users actually debug than Idris-side stringification. Needs a `tensor_save_npy : AnyPtr -> String -> ()` primitive in all three C backends + a `Network`-level walker like `debugSaveLayerOutputs : Nat -> Network i hs o d -> Tensor [i] d -> IO (Network, Tensor [o] d)` that writes per-layer outputs as `<epoch>-<layer>.npy`. The lightweight stderr tracer (`forwardVarTraced` in `Tensor.idr`) covers the "where did the NaN come from" use case; this ticket is for the "I want to inspect activation distributions over training" workflow. File once a real R&D need surfaces |
| Tape optimizer foreach fast path | perf | M | Tape's `tape_optimizer_step` (in `backend_tape/training/optimizer.c`) walks the registered params per-element to apply SGD / RMSprop / Adam / AdamW updates — the same scalar loop the shared port-default uses. Could switch to a batched / "foreach" impl using BLAS-1 primitives (`cblas_daxpy`, `cblas_dscal`, `cblas_dnrm2`) or vDSP element-wise on the flat `opt->m` / `opt->v` buffers; the shared `Optimizer` struct already exposes them. Torch's `at::_foreach_adam` is the reference for what "foreach Adam" looks like as a fused multi-tensor kernel; tape's equivalent is fully expressible in stock BLAS (no fused kernels needed because m/v are flat doubles). Path: write a `tape_optimizer_step_foreach` variant under tape's adapter that does the same math via BLAS-1; gate by env var or LR-schedule heuristic; benchmark vs per-element to verify the win. Doesn't change the port surface (still binds the same `optimizer_step` port slot). The shared optimizer's "default per-element loop" remains for backends without a vectorized path. Cross-ref: tape's per-element loop at `backend_tape/training/optimizer.c` lines ~170-220, the Adam case at ~205. Bench harness: `scripts/perf-run.sh example-supervised tape` baseline; LSTM / Transformer / NTM as Adam-heavy workloads. |
| Reconsider build / link / runtime backend model | refactor | M–L | The port/adapter pattern landed alongside Phases 2-4 (`packages/backends/shared/training/port.h` 39-method `BackendPort` struct + per-backend `g_active_port_<b>` via rename header) makes alternative wire-up models tractable. Today: all backends compile into one `libidrisml.dylib` via multi-link (`BACKEND=tape,torch,mlx`); the rename header makes each backend's exported symbols unique (`tensor_add_tape`, `tensor_add_torch`, etc.). Alternatives worth exploring: **(a) plugin / dlopen** — one `.dylib` per backend (`libidrisml-tape.dylib`, `libidrisml-torch.dylib`, ...), loaded at runtime via `dlopen` from the Idris-side device wrapper; eliminates the rename-header machinery; gives backend-specific lazy-load (no libtorch in memory for tape-only sessions); needs a small symbol-table abstraction. **(b) per-backend static archives** — each backend builds to a `.a` that the consumer links explicitly; clean but adds friction. **(c) per-backend processes + shared-mem IPC** — overkill but isolates crashes. **(d) keep multi-link, rework around the port** — the rename headers (231 entries) exist because every C function has a per-backend export; with the port abstracting most cross-backend dispatch, could the rename surface shrink to just the port struct + a few entry points? Each option has trade-offs against load time, link complexity, build matrix size, and dev ergonomics. Big-design item; warrants its own plan-mode session that ends with a concrete recommendation backed by prototypes. |
| Gate debug/observability logging behind a build flag | refactor | M | RSS + live/peak handle-count logging (`get_rss_mb`/`get_current_rss_mb`, `tensor_live_count`/`tensor_peak_live_count`, surfaced as `peak=`/`cur=`/`handles=` per-epoch and `Peak RSS`/`Live handles`/`Peak handles` in the summary) is always compiled and always called from `runTrainingIO`'s `logEpoch` + timing summary. A production build shouldn't even *contain* this code — it should be excluded at compile time, not just skipped at runtime. The peak-handle tracking also adds a per-Tensor-construction comparison in each backend's ctor/append path (`g_*_peak_live` update) — cheap, but pure observability. Want a build flag (e.g. `make IDRISML_DEBUG_LOGGING=1` setting a `-DIDRISML_DEBUG_LOGGING` on the C side + a generated Idris config like `BuildConfig`'s device/dtype selection) so: (a) the C peak-tracking + `*_live_count` bodies compile to no-ops (or aren't emitted) in prod; (b) the Idris `logEpoch`/summary omit the handle/RSS calls in prod; (c) the default dev build keeps them. Open questions: do we gate RSS too (it's been useful and cheap) or only the new handle counters? how does the Idris side observe the flag at elaboration (same trick as `BuildConfig` — bake into a generated module)? Keep the FFI decls available so a debug build links them; just don't *call* them in prod. Cross-ref: the lifecycle work (`docs/develop/tensor-lifecycle.md`) that motivated the handle counters — note in the doc that the counters are debug-gated once this lands. |
| Reconsider layer / module naming | refactor | M | The current shape — `Layer/` (singular directory holding many layer types), `linearLayerAny`/`tanhLayerAny`/`lstmLayerAny` constructors with the awkward `*Any` suffix, top-level `Layer.idr` umbrella module re-exporting everything, `Network` + `OutputLayer` for the chain — accumulated as the `LayerLike`+`AnyLayer` pattern (`Layer/Core.idr:22-72`) was bolted on. The naming honesty problem: a `Tanh` is plausibly a "layer", a `Transformer`/`Ntm`/`Dnc` is a sub-network with attention + norm + FFN + residuals + state and stretches the term. PyTorch's solution is `nn.Module` as the compositional base; the theory-grounded alternative is `Para` (the Para-categorical view of NN components as parametric morphisms — see Fong/Spivak 2019 and Glaive's "Building a Neural Network from First Principles using Free Categories and Para(Optic)" post). Candidate axes: (a) `*LayerAny` suffix → bare names (123 call sites, mechanical); (b) `Network` → `Sequential` / `Path` / `Composition`; (c) `OutputLayer` → `Last` (or eliminate via identity-morphism if we go full CT); (d) `LayerLike` → `Module` / `Para` / `Morphism` / `Lens`; (e) `AnyLayer` → corresponding existential; (f) `Layer/` dir → `Nn/` / `Para/` / `Modules/`; (g) split `Network` machinery out of `Layer.Core`. **Deferred pending the Glaive survey** (Low Priority row above) — Glaive's published Idris formalisms (free categories, Para/Optic, categorical autodiff) may dictate which framing to adopt, and committing to a naming scheme before that audit risks renaming twice. Pick up after the survey lands its "adopt / borrow / inspiration only" verdicts. |
| Audit PyTorch design decisions for ideas to borrow | research | M | The kernel-internals survey (`docs/develop/pytorch-survey.md`) covers PyTorch *internals* (dispatcher, kernel-cache, allocator, autograd graph) for portable kernel-level wins. This is the broader complement: a survey of PyTorch's *design decisions* — API shape, abstraction boundaries, ergonomic affordances — for goodies to pinch. Things to look at: (1) `nn.Module.__call__` vs `forward` dispatch and the hook system; (2) optimizer param groups, LR schedulers, the `state_dict`/`load_state_dict` contract; (3) loss-as-class vs loss-as-function dual API; (4) `DataLoader`/`Dataset`/`Sampler` separation of concerns; (5) `.to(device)` ergonomics + device propagation rules; (6) `autocast` + `GradScaler` for mixed precision; (7) `torch.profiler` and FLOPS counters; (8) `torch.compile` / `fx` / `jit` for the type-system-friendly subset (graph capture and replay is interesting in a typed setting); (9) `nn.functional` vs `nn.Module` mirror (each layer has both forms); (10) `register_buffer` vs `register_parameter` (non-learnable vs learnable state, well-defined save/load). For each, a short "adopt / borrow interface / inspiration only / skip" verdict. Deliverable: `docs/develop/pytorch-design-survey.md` (separate from `pytorch-survey.md` which is the kernel-internals one). Most goodies are probably worth picking up incrementally — file follow-up tickets for the ones that score "adopt". |
| Opaque type-level Nats | external | M–XL | Idris 2 Peano Nats hang the compiler for dims > ~1000 and for multiplicative shape literals. Blocks identity layers (dropout, batch norm) at large dims; forces every multiplicative dim through `TVec`/`TMat` aliases. Need machine-backed type-level naturals (like GHC TypeLits). **2026-05-26 update**: re-examined and the *actual* blocker may not be the upstream language design — Idris 2 has shipped relevant compiler work (improvements to `Nat` elaboration / built-in `Natural` handling) that we are not currently picking up because the **nix distribution doesn't build the stdlib** as part of the Idris 2 package. The compiler we run is recent but its `Data.Nat` (and possibly other) modules are the as-shipped version, not what's currently on `idris-lang/Idris2:main`. Action items, cheapest first: **(a) Audit the gap** — diff the nix-installed `Data.Nat.idr` against upstream `idris-lang/Idris2/libs/base/Data/Nat.idr`; identify which of the type-level-Nat / Peano-recursion gotchas in `docs/develop/gotchas.md` would be cleared by the upstream version. **(b) Rebuild stdlib locally** — branch idris-ml's nix expression to also build (or override-import) the upstream stdlib at a pinned commit; verify our gotchas-list goes down. **(c) Push upstream to nixpkgs** — if (b) confirms the fix, the long-term answer is a nixpkgs update so other users get the same experience; until then a local override doc'd in the README is fine. **(d) Only if (a)-(c) don't close the gap** does the original "engage with Idris 2 upstream on machine-backed TypeLits" remain as the long-pole work. Subsumes the previously-separate "Upstream Data.Nat helpers to use Integer arithmetic" row (deleted 2026-05-26) — that row's `Data.Nat.lte` / `divNat` / `modNatNZ` recursive-Peano fix is the same upstream patch we'd be pulling in via (b). See `docs/develop/gotchas.md` (the "Large Nat type-level reduction" and "Data.Nat stdlib functions are recursive at runtime too" entries) and the `perf-changes.md` 2026-05-14 entries for the perf impact. |
| HPO B4 — Network-structure tuning (width/depth) | research | M | For each example with a tunable hidden size (FC, RNN, LSTM, Transformer, NTM, DNC), grid over `H ∈ {16, 32, 64, 128}` (and depth where applicable) using the A3 sweep runner. Goal: smallest network that converges to the target metric — reduces compile + train time without sacrificing the demonstration. Each `H` change triggers an Idris recompile but 4 builds per example is manageable. Out-of-scope: changing the *layer type* (e.g. swapping LSTM → Transformer); that's a coverage decision in B1. See HPO plan B4 |
| GRU C kernel: implement standard reset gate | feature | S | Current `tensor_gru_cell` (across tape/MLX/torch) is a simplified GRU variant — r is computed but NOT used to mask the n-gate's hidden contribution. PyTorch's `nn.GRUCell` and the GRU literature use `n = tanh(W_in x + b_in + r * (W_hn h + b_hn))`. Both Idris (`Layer/Gru.idr` `applyGeneric` and the C kernel) and PyTorch reference (`LinearGRUCell` in `models/rnn.py`) agree on the simplified variant — this is an alignment between our two backends, not a divergence. Documented in `docs/develop/reference-alignment.md` "GRU example added". Sub-tasks if/when prioritized: update C kernel forward + backward across all 3 backends, update `applyGeneric` in `Layer/Gru.idr`, update `LinearGRUCell.forward` in `models/rnn.py`, re-validate convergence across all 5 seeds. Convergence already strong at simplified variant (5/5 both backends, ~1300 epochs to loss < 1e-3) so this is correctness/standardness, not a performance issue |
| Localize mlx-gpu BF16 vs F32 wall gap on HfLlama-1B inference (was: audit mlx fused-op constant pool) | perf | M | **Reframed 2026-06-06**: the originally hypothesised cause — that ~26-28 hardcoded `mx::float32` constants in fused-op kernels were forcing promote-narrow cast traffic on BF16 operands — was audited and ruled out. All 28 `mx::float32` hits across `packages/backends/backend_mlx/` are already correctly mixed-dtype-aware: dropout's F32 RNG chain (mlx-mandated; mx::random::uniform is F32-only on Metal) casts to operand dtype via `mx::astype` *before* the multiply at `nn/norm/dropout.cpp:23`; the optimizer's `opt_dtype = mx::float32` default at `training/optimizer.cpp:291` is overridden by the dtype-discovery loop at lines 292-298 that reads the first eligible param's dtype; `kF32_ZERO/ONE/HALF` singletons in `precision.h:35-37` are only consumed by dropout (mixed-dtype-correct) and cast-replay (where F32 is the intentional staging); the F32 stage in BF16/F16 narrowing constructors (`precision.h:79,92,104`) is intentional widening before `astype` narrowing. Crucially, `runGenerate` (the path that produced the +62% measurement) is **inference**, which bypasses dropout entirely via the early-exit at `dropout.cpp:17` (`if (!training || p <= 0.0) return hinput;`) — so dropout constants can't be the cause regardless. **Real-cause hypotheses to audit** (in rough order of likelihood): (a) **mlx-Metal kernel-level BF16 vs F32 perf** — mlx's Metal kernels for matmul / softmax / silu may be inherently slower at BF16 than F32 on the M4 hardware (mlx's BF16 Metal kernels are newer and may not have the same optimization passes). (b) **Embedding table lookup** — the `[vocab=128256, dim=2048]` embed_tokens matrix is loaded as BF16 in BF16 mode; the per-decode-step lookup may hit a less-optimized BF16 gather path. (c) **RoPE position-encoding tables** — `buildLlamaRoPETables` builds cos/sin tables; verify they're matching operand dtype rather than always-F32. (d) **Attention scale** — `1/sqrt(d)` divisor inside SDPA; verify it's matching operand dtype. (e) **RMSNorm epsilon** — verify `applyRmsNorm2dRaw`'s eps constant matches operand dtype. **Approach**: re-measure BF16 vs F32 on the current codebase first (the original +62% was at `6bf2ca8+dirty` ~2 weeks ago; many changes since); then profile per-op cost on both, looking for ops that are slower on BF16 by >20%. Compare against `MLX_DEVICE=cpu` BF16 vs F32 to factor out the Metal-specific component. **Target**: explain the BF16 vs F32 wall gap to within ±20%; ideally close it. Cross-references: `docs/develop/perf-changes.md` 2026-05-31 entry (the +62% measurement); `packages/idris-ml/src/Device/Mlx.idr:658-668` (now-corrected constant-pool comment block); `backend_mlx/nn/norm/dropout.cpp:17` (training-mode early-exit that invalidates the original hypothesis on the inference path); `backend_mlx/training/optimizer.cpp:286-298` (dtype-discovery loop already in place). |
| DRY-up the `idris-transformers` adapter package | refactor | M | The four shipped adapters (`HfBert.idr` 33 KB, `HfGpt2.idr` 24 KB, `HfLlama.idr` 27 KB, `HfBitNet.idr` 42 KB; 83 / 93 / 117 / 184 exported declarations respectively) accumulated independently and each carries its own private re-implementation of patterns that are structurally identical across two or more adapters. The `CONVENTIONS.md` rule "param names + storage shapes match HF on disk" stays load-bearing — generic loader+remap machinery is explicitly rejected — but plenty of the *shape* of an adapter (block forward, norm wrappers, LM-head projection, param-name catalogues, test-side machinery) is mechanical and pure-Idris and can move into shared helpers without touching the per-arch on-disk-naming contract. Concrete consolidation candidates, ordered by leverage: (a) ~~**RMSNorm application** — Llama + BitNet both apply RMSNorm pre-attn + pre-MLP + final; the implementation in both is essentially `applyRmsNorm w eps x = let inv = rsqrt(mean(x*x) + eps) in x * inv * w`; bring to one helper, both adapters call it.~~ **SHIPPED** as `HfCommon.applyRmsNorm2dRaw` 2026-06-03 — initial extraction (`080409c`) then collapsed to a single FFI call against the new `primRmsNorm2d` typeclass (`416c011`). (b) **Pre-norm decoder-block scaffold** — Llama + BitNet both run `norm → attn → residual → norm → mlp → residual`; the difference is the per-arch attention + MLP component types, not the chain shape. A `decoderBlockPreNorm` skeleton parameterised by an attention closure + an MLP closure deletes ~50 lines from each adapter. (c) **GQA-friendly attention plumbing** — both Llama and BitNet split fused QKV with the same GQA arithmetic (`numHeads`, `numKvHeads`, `headDim`, repeat-KV for GQA broadcast); the slicing + RoPE-apply + scaled-dot-product call sequence is identical modulo the BitLinear-vs-Linear weight type. (d) **Tied-LM-head projection** — both Llama (untied) and BitNet (tied) use `embed_tokens.weight` shape `[vocab, hidden]` as the LM-head projection matrix when tied; one `projectTiedLmHead model.embedTokens.weight hFinal zeroBias` helper covers both. (e) **Param-name catalogue generators** — every adapter has a `hfXxxParamNames` returning a `List String` and an `expectedXxx_ParamNames` test catalogue. The generator shape is identical (concat per-block prefixes with per-component suffixes); a `forBlocks numLayers (\i => …)` combinator + per-arch component lists could share infrastructure. (f) **Test-side `testParamCount`** — each adapter's test file open-codes the count + per-name set check. One shared `assertHfModelParamCount` driver parameterised by `(modelStateBuilder, expected names)`. (g) **RoPE-table wiring** — Llama and BitNet both call `buildLlamaRoPETables` from `Layer/RoPE`; that's already shared, but the per-arch `RopeBase` + `RopeScaling` config lives in the example. Capture in a per-arch config record exposed by the adapter so examples don't re-declare. (h) **Inference example scaffolding** — `HfLlamaInference.idr` and `HfBitNetInference.idr` (and the new `HfBitNetInference` after the greedy-decode addition) duplicate ~150 lines of identical machinery: `mkIds`, `printRow`, `toExistVect`, `argmaxRow`, `genOneStep`, `genLoop`, `runGenerate`, `stageStamp`, the argv-parse helpers (`extractPrompt`, `extractNumTokens`). Factor into a `HfInference` skeleton in `idris-transformers` (or `Example.Common.HfInferenceHelper` in the examples package) so each per-arch example becomes ~50 lines of config + forward-pass selector. **Out of scope (load-bearing per-arch, must stay per-adapter)**: HF param-name string literals, on-disk shape catalogues, the `BitNetModelState` / `LlamaModelState` / `BertModelState` record shape itself (the field set is part of the per-arch contract). **Approach**: incremental and TDD-driven — pick one consolidation candidate, write a test asserting the per-arch behaviour is unchanged, factor, run the full `test-transformers` + `test-hf-*-roundtrip` suite, commit. Don't try to land all 8 in one PR. (a)+(b)+(d) are the highest-leverage and probably account for most of the savings; (e)+(f) tighten the test-side; (h) is the most user-visible (the examples are how users discover this library). **Expected outcome**: HfBitNet 42 KB → ~25 KB, HfLlama 27 KB → ~18 KB, each new-arch adapter starts at ~10 KB instead of ~25 KB. Cross-references: `packages/idris-transformers/CONVENTIONS.md` (the load-bearing constraints — don't violate); `Layer/RoPE.idr` (existing shared helper that proves the consolidation pattern works); `Layer/Transformer.idr` (idris-ml's from-scratch transformer is intentionally separate per CONVENTIONS, do not merge with). |
| Reduce idris2/Chez elaboration memory peak on heavy-implicit examples | refactor | M | **Symptom**: `HfLlamaInference.idr` peaks Chez at ~17 GB during the `idris2 -o hf-llama-inference …` elaboration step on a 16 GB Tart VM — pushes ~1 GB into compressed swap and slows host responsiveness. Previously peaked at ~23 GB before the dual-decode-path simplification (`95adc631` 2026-06-04 dropped `genOneStep`/`genLoop` and the `case useCache of` branches, saving ~30% by halving the implicit-chain resolution work). Still above the physical-RAM line for full headroom. **Root cause**: Idris's elaborator resolves the per-call-site implicit chain for the cache-aware forward — `numHeads × numKvHeads × headDim × intermediate × maxPos × vocab × hidden × numLayers + 8 typeclass instances (UserDeviceTraining / UserDeviceCore / Compatible / Linked / RuntimeDType / GradMode / ...)` — through every layer in `genStepCached → hfLlamaForwardLmStep → hfLlamaForwardStep → applyBlocksCached → applyBlockCached → applyAttentionCached → ropeAllHeadsFlat → applyRopeAllHeads`. Each call site re-resolves; the unifier holds Chez closures for the in-flight resolution state. Dtype is NOT a factor here — F32 vs F64 in `BuildConfig` is a phantom-type swap with no Chez memory delta (verified by inspection 2026-06-04). **Levers in order of leverage** (each independent; pick one and measure): **(a)** Move `genStepCached` + `genLoopCached` from `HfLlamaInference.idr` into a new top-level `Example.HfLlamaCacheGen` module. Each idris2 invocation then has half the implicit chain to resolve. Same total work, lower per-process peak. Cheap, ~50 lines of refactor. **(b)** Lift the cache-aware forward bodies up to `idris-transformers/src/HfLlama.idr` (where `hfLlamaForwardLmStep` already lives) as pre-elaborated library functions; the example file just calls them with concrete Llama-3.2-1B dims. Pre-elaborated TTC caches the resolution at the library-install step; the per-example compile doesn't re-resolve. Cheap, moves ~70 lines. **(c)** Concrete-Nat specialization at definition time — define `genStepCached_Llama32_1B` etc. with concrete `numHeads=32 / numKvHeads=8 / headDim=64 / hidden=2048 / numLayers=16 / intermediate=8192 / maxPos=8192` bound at the top level. The unifier doesn't re-solve at every call site since the dims aren't polymorphic. Trades polymorphism for elaboration speed — HfGpt2/HfBitNet cache landings would each need their own specialized copy. Probably the biggest peak drop but most surface impact. **(d)** Audit the implicit chain for unnecessary 0-quantity slots — every implicit that doesn't actually need to be tracked is one less unifier state. Long tail of small wins. **(e)** Same fix on the C++ side as a separate concern (PCH + parallel compile cap via `MAKEFLAGS=-j2` already in `scripts/perf-run-quiet.sh`); not directly related but compounds with this. **Cross-references**: `docs/develop/gotchas.md` "Polymorphic type-parameter slot vs concrete value in method body" (the prior 30+ GB blowup that mandated keeping `dt` polymorphic — the lesson generalizes: every polymorphic slot is unifier surface area); `feedback_typeclass_zero_arg_method_eval` (zero-arg typeclass methods firing eagerly is a related Chez cost); the dual-decode-path drop in commit `95adc631` which set the precedent for "one canonical path"; `scripts/perf-run-quiet.sh` (C++-side mitigations + caffeinate). **When to pick this up**: when a current workload's compile is blocking interactive use, or when HfGpt2/HfBitNet land their cache variants and the same pathology re-emerges on those files. Until then, the `perf-run-quiet.sh` wrapper + caffeinate + the dual-path drop give enough headroom that compiles succeed even if uncomfortable. |
| Match PyTorch's fused-op catalogue on torch backend (closes the ~150× torch-mps Llama gap) | perf | L | **Rescoped 2026-05-29 from "deferred-op tape (XL)" after measuring PyTorch Python head-to-head** (`docs/develop/perf-log-ref.jsonl` + `perf-changes.md` 2026-05-29 entry). PyTorch Python on the SAME libtorch + MPS device does the same 8-token Llama-3.2-1B `use_cache=False` decode in **2 seconds** of generate-time vs idris-ml's **5 min 07 s** — a ~150× gap. This refutes the closed #393 row's "structural ceiling" reading: the per-op MTLCommandBuffer submission cost (~1.89 ms/op) is real, but our **op count (~19,400/forward) is 30-100× higher than PyTorch's** because we decompose into primitive ops where PyTorch lands a single fused op. The fix is *not* a deferred-op tape (architectural rewrite) — it's matching PyTorch's catalogue of high-level fused ops where it materially shrinks our op count. **Prime suspects** (ordered by likely impact): (1) **attention — SHIPPED** (`6850366` 2026-05-30 SDPA all 3 backends + `c09d374` all-heads RoPE; op count 18,410 → 2,634/step on torch-mps, -86%; mlx-gpu wall 45.5 s → 16 s, 2.8×). (2) **RMSNorm — SHIPPED** (`416c011` 2026-06-03 `primRmsNorm2d` typeclass + 3 per-backend impls; replaces per-row 8-prim chain in HfCommon.applyRmsNorm2dRaw; cross-language gate PASS on torch-mps 4.96e-05 and mlx-gpu 1.20e-04 max-abs-diff). (3) ~~**SwiGLU MLP** — fuse `silu(up(x)) * gate(x)` into one op (libtorch has `at::silu` but not the fused multiplication; may need a small C++ wrapper).~~ **SHIPPED** (`24517c8` 2026-06-03 `primSwiGlu2d` typeclass + 3 per-backend impls; replaces `tsilu g; tmul sg u` pair in `HfLlama.applyMlp`; cross-language gate PASS on torch-mps 4.96e-05 and mlx-gpu 1.20e-04 max-abs-diff, both bit-identical to RMSNorm baseline; torch-mps op count 918 → 902 (-16/step exactly as predicted); mlx-gpu wall 63 s → 49.2 s (-22%); torch-mps wall noisy across trials). (4) ~~**Embedding lookup + reshape** — verify what `at::embedding` exposes and whether our wrap introduces unnecessary extra `from_tensor` calls.~~ **SHIPPED** (`ca6f9ab` 2026-06-03 `primEmbedding2d` typeclass + 3 per-backend impls; `tensor_embedding` flattened to 1D so every caller called `primReshape2d` to reshape back to 2D — two ops where one would do; new variant returns the natural `[n, embedDim]` directly; cross-language gate PASS on torch-mps 4.96e-05 and mlx-gpu 1.20e-04 max-abs-diff, both bit-identical; op count 902 → 901 (-1/step exactly as predicted)). **Approach**: per-op-fusion lands as a focused FFI primitive in `backend_torch/nn/<category>/`, an instance method on a new `FusedOps` typeclass (or extended `UserDeviceTraining`) with no-op fallbacks to the existing primitive chain on tape + mlx, and a smart constructor in `Tensor.idr` that calls the typeclass. **Per-fusion measurement**: the existing `tensor_perf_op_count` counter from commit `e9763d0` is the metric — pre/post comparison shows whether the op-count drop closed the gap. Land fusion-by-fusion (each commit closes some specific fraction of the gap). **Why L not XL**: each fused op is independently scoped, ~few hundred LOC each, no architectural change to autograd or the smart-constructor surface — just adding new typeclass methods alongside existing ones. **What this is NOT**: no deferred-op tape, no graph-mode Tensor, no changes to backward or autograd integration. KV cache work (filed separately as "HfLlama KV cache + Llama 3.2 1B end-to-end gate") is orthogonal and stacks with this row. Cross-references: `perf-log-ref.jsonl` 2026-05-29 entries (PyTorch Python baseline), `docs/develop/perf-changes.md` 2026-05-29 entry, `packages/idris-transformers/scripts/time_inference_llama.py` (the canonical head-to-head). |
| HF auth for `hf_llama_inference.py` paired ref | perf | S | `BENCHMARKS.md`'s `hf-llama-generate-inference` row lands at 47.75 s/token on the Idris side but the PyTorch ref column is empty — the Python script at `packages/pytorch/torch_ref/scripts/hf_llama_inference.py` couldn't pull `meta-llama/Llama-3.2-1B` (gated HF Hub model needing an access token). Two fixes: (a) wire HF_TOKEN through pip env so the box has auth, OR (b) swap to a public Llama-architecture model (`unsloth/Llama-3.2-1B`, `RWKV/llama-r1-1b`, …) and verify both Idris and the PyTorch ref agree on the same weights. (b) is the easier path. Trigger: next time you want a Llama PyTorch baseline number. |
| Investigate: does Idris 2 ship (or have community) a linter? | research | S | The Python surface gets `ruff` + `vulture` (PR-gated, 2026-06-05) and the C / C++ surface gets `cppcheck` + `clang-tidy` (PR-gated, 2026-06-05), but the *Idris* surface — by far the largest in this repo — has no dead-code / unused-import / style gate today. Investigate: (a) does idris2's own compiler emit unused-import warnings or unused-binding warnings under a flag we're not setting? (b) does `pack` carry a lint subcommand or lint-adjacent target? (c) any community linter projects (`idris2-lint`, `idris-format`, similar)? (d) is there a path via the compiler API (Idris.Core / Compiler.RefC / etc.) to write a small Idris linter that catches the bug classes the codebase has repeatedly hit (unused parameters, dead `where` clauses, shadowing, `believe_me` / `unsafePerformIO` regressions, paramId-less Tensor construction, etc.)? **Deliverable**: a half-day spike with a written-up "options + recommendation" in `docs/develop/idris-lint-survey.md`. Worth the investigation because the existing static-analysis gates only cover the C / Python sub-surface; the Idris-side bug classes (per `docs/develop/gotchas.md`) are where this codebase has spent the most rediscovery time. Cross-refs: existing `test-integration-lint-non-io-side-effects` (custom Python script that greps Idris sources for a specific bug class — proves the "lint via external tooling" path is viable), `docs/develop/gotchas.md`. |

## Low Priority

| Item | Tag | Difficulty | Notes |
|------|-----|-----------|-------|
| Tape F64 large-LM OOM (HfLlama + HfBitNet) — ~15 GB working set on 16 GB VM | bug | M | Affects both HfLlama-3.2-1B and HfBitNet-2B-4T on the tape backend's default F64 lingua-franca. Same root cause both times: 1B+ params × 8 bytes/value + per-forward arena growth pushes the working set past the 16 GB Tart VM ceiling, the next chunk's `malloc` returns NULL, `arena_alloc` returns garbage, and the Idris-side derefs SIGSEGV — surfacing as Chez's catchall **`Exception: invalid memory reference. Some debugging context lost`**. **HfLlama observed 2026-05-29**: crash on step-7 of greedy decode (`tokenizer probe` 3 s, `hfLlamaModel` 16 s = ~10 GB malloc of 146 F64 param tensors, `loadModelAllowCast` 27 s, `runGenerate` step-6 forward completes, step-7 crashes); 1.24 B params × 8 B = ~10 GB params, last-forward arena ~5 GB, working set ~15 GB. **HfBitNet observed 2026-06-02** (post-numerical-match fix): tape lane in the cross-lane perf sweep crashes on the FIRST BitLinear forward inside `runGenerate` (`tokenizer probe` 4 s, `hfBitnetModel` 11 s, `loadHfBitnetCheckpoint` 16 s, `runGenerate` first BitLinear triggers `[tape diag] op=3 general_bcast shapes: a=[1,2560] b=[2560]` then immediately SIGSEGV); embed_tokens [128256, 2560] × 8 B = 2.6 GB; on-disk-packed 210 ternary weights stay packed (~280 MB) but each per-forward dequant materialises a [out, in] F64 intermediate (~6.5 MB × 210 = 1.4 GB per forward step) which combined with embedding + RmsNorm intermediates + accumulator buffers blows past the 16 GB ceiling earlier than HfLlama does. **In both cases**: `resetForEval` between iterations only zeroes the arena's `used` counters (chunks kept), so the FIRST arena_new_chunk that NULL-mallocs is the trigger; subsequent arena_alloc reads garbage. `TAPE_DTYPE=F32` would halve the param footprint on HfLlama (~7.5 GB working set, comfortable) but only fixes part of HfBitNet (the F64 intermediates during dequant remain F32 if we route there). **BF16 mode on tape doesn't help** — tape's BF16/F16 are lingua-franca dtypes whose internal storage is still F64. F32 is the only sub-F64 path that actually halves tape storage. **Fix options**: (a) document tape F64 large-LM as out-of-scope on 16 GB VMs (cheapest — matches the existing KV-cache row's "tape F64 doesn't fit; deliberately skipped" note); (b) add `arena_free_chunks_keeping_first()` so `resetForEval` releases chunks beyond a working budget between forwards (gives F64 some headroom but doesn't make 10 GB params smaller); (c) mmap-back persistent params from the safetensors file instead of malloc+memcpy (frees ~10 GB of RSS for inference workloads where params are read-only). The user-visible fix for now: run with `TAPE_DTYPE=F32` (HfLlama is known-good; HfBitNet on tape-F32 is untested but should fit). **Open question for HfBitNet specifically**: would packed-ternary storage on tape (instead of unpacked-during-dequant) eliminate the per-forward 1.4 GB intermediate? The 2026-06-01 sub-byte arena (`DT_TERNARY` enum) was reserved for this but the current `tensor_bitlinear_fwd_hf_quant_tape_*` still decodes inline per-row, materialising a F64 temporary. A packed-everywhere variant would avoid the arena spike. Cross-references: existing perf-log entries `2026-06-02T22:47:23` (tape OOM observation, commit `d9510bb`); the HfBitNet F32 path is not yet validated and is the obvious next fix-direction. |
| Upgrade tokenizer backing: Rust FFI in a new `packages/idris-tokenizers/` package | perf | M | `Tokenizer.idr` (landed 2026-05-26) backs onto a Python subprocess via `scripts/hf_tokenize.py` — each call is ~1 s of Python startup. Acceptable for the BERT (3 sentences) + GPT-2 (1 prompt + 1 detokenize per generation) patterns; would become painful if (a) a workload tokenizes many short strings per second, OR (b) a non-HF Idris example wants tokenization. Upgrade: vendor `huggingface/tokenizers` (the Rust crate) as a git submodule under `packages/idris-tokenizers/vendor/`; build a tiny C ABI exposing `tokenizer_new`/`tokenizer_encode`/`tokenizer_decode`/`tokenizer_free`; expose as `libidristokenizers.dylib`; load via `%foreign "C:..."` from a new `packages/idris-tokenizers/` peer package (separate from `idris-transformers` because tokenization is a standalone capability — anyone writing a from-scratch char-LM benefits). Drops per-call cost from ~1 s to ~50 µs. The `Tokenizer.idr` API (`Tokenizer vocab` GADT + `mkTokenizer`/`tokenize`/`detokenize`) stays exactly the same — Liskov-substitutable, callers don't change. Trigger to land: (a) a user reports the subprocess startup as a real perf issue, OR (b) a non-HF Idris example wants tokenization. Investigation done 2026-05-26 ruled out the nixpkgs bootstrap-stage2 patch as the binding constraint on the Nat-pattern OOM (see gotchas.md "Pattern-matching `Nat` literals"); that ruled-out memo applies regardless of whether this row eventually lands. |
| transformer mlx-gpu intermittent crash in `mlx_sweep_generation` (+ mlx allocator failures on long-budget RL) | bug | M | **Additional manifestation 2026-06-04**: full perf-sweep at the post-narrow-fix commit `4c7aa76f` exposed a second mlx memory-class failure mode — `mountain-car-cont` and `sac` (the two N_LONG=2000 RL examples) crash on BOTH mlx-cpu and mlx-gpu with `std::runtime_error: [malloc] Unable to allocate {4, 256, 512} bytes`. Allocating 4 bytes does NOT genuinely OOM — the 16 GB VM has plenty of headroom. This smells like mlx's internal allocator pool getting corrupted / exhausted after 2000 epochs of accumulated tape entries (the same long-run-accumulation suspicion as the main row, just landing as allocator-OOM rather than ArrayDesc-poison). All other 14 examples × mlx-cpu/mlx-gpu cells ran clean (28/30 cells). Per-cell symptoms: `mountain-car-cont/mlx-cpu` aborted with 512-byte alloc; `mountain-car-cont/mlx-gpu` 256-byte; `sac/mlx-cpu` 4-byte; `sac/mlx-gpu` 4-byte. Likely shares root cause with this row's main investigation track (mlx tape entry retention through long runs). Reproducer: `caffeinate -i nice -n 19 env MAKEFLAGS=-j2 make BACKEND=mlx MLX_DEVICE=cpu example-mountain-car-cont MOUNTAIN_CAR_CONT_ARGS="--epochs 2000 --seed 42"`. ── **Original symptom**: An intermittent crash (SIGBUS or, less commonly, SIGTRAP) fires on `BACKEND=mlx MLX_DEVICE=gpu ./build/exec/transformer`; not accumulation-driven (was misdiagnosed as a 200-epoch bug). Crash-trace handler (`MLX_CRASH_TRACE=1`, `init.cpp`) caught representative SIGBUS backtraces — all at the **same** address `0x5200000081` (a stable freed-memory poison value, not random garbage), inside `mlx::core::array::ArrayDesc::~ArrayDesc` recursing two levels deep, called from `mlx::core::array::~array`, called from `mlx_sweep_generation` at the `delete t` path → `~Tensor` → `~mx::array` cascade. The recursion enters at the same `+504` offset both levels — i.e. ArrayDesc's destructor walking its `std::vector<array> inputs` field, calling `~array` on a child which calls its own `~ArrayDesc`, which then reads a field at offset 504 from a freed-and-recycled ArrayDesc. **Stable poison-address signature → stale `shared_ptr<ArrayDesc>` somewhere (control block still alive, managed object gone), not heap-random corruption.** Mitigation landed 2026-05-25 (`140bd14`): `mlx_sweep_generation` evals BOTH `t->data` and `t->grad` (when `has_grad`) before the deletion loop, so `~mx::array` is mostly a leaf decref. Re-measurement 2026-05-25 post-multi-link refactor: 6/600 (~1%) at `--epochs 5` varied seed, 3/400 at `--epochs 1 --seed 42` — i.e. substantially below the originally-recorded 4/100, but non-zero. **Fix attempts that BACKFIRED** (each tested at 400 runs vs the ~1% baseline): (a) post-eval `array::detach()` on every live array — climbed to 20/400 (5%); (b) post-eval `array::wait()` — climbed to 23/400 (5.75%). **Diagnostic from those failures**: touching ArrayDesc fields beyond eval surfaces *more* corruption, which means eval is forgiving of already-stale-but-`Status::available` ArrayDescs (skips the read), while detach/wait reach into `inputs/siblings/primitive` and trip the latent stale-shared_ptr. So the corruption is created somewhere earlier (a per-op replay, vjp's tracer chain, or an mlx-internal cache holding raw ArrayDesc pointers) — not by the sweep itself. Investigation continuations: (1) bisect with `MallocStackLogging=1` + `MallocScribble=1` to capture the prior-free site of the recycled ArrayDesc; (2) audit per-op meta-payload sites (`tape.cpp` op-meta delete-cases) for any path that drops an `mx::array` shared between meta and tape entry; (3) try a delete-as-husk pattern (overwrite `t->data` / `t->grad` with `g_husk_empty` before `delete t`, so `~Tensor` only destructs the husk). Reproducer: `for i in $(seq 1 400); do stdbuf -oL ./build/exec/transformer --epochs 5 --seed $i >/dev/null 2>&1 || echo CRASH; done | wc -l`. Per `feedback_memory_corruption_is_p1` this stays P1 until 0/N. |
| Multi-format serialization adapter (GGUF / ONNX port) | feature | L | The checkpoint layer (closed 2026-05-21) is deliberately **safetensors-only** — it's the de-facto standard (HF Hub default, now under the PyTorch Foundation) and the load/save path is structured as a clean seam, but no format-adapter abstraction was built (it'd be speculative). If a concrete need for another on-disk format appears, introduce a `SerializationFormat` port (read/write tensor-name → bytes+dtype+shape) with safetensors as the first impl, then add a second backend. Candidate formats: **GGUF** (llama.cpp / local-inference ecosystem — quantized weight blocks, would pair with the LLM-class example for loading community quantized checkpoints); **ONNX** (interop with non-PyTorch toolchains — but ONNX is a graph+weights format, so this is much larger than a weight serializer and probably out of scope). Pickle/`.bin` is explicitly *not* a target (insecure, hard to parse safely in Idris, and HF has migrated off it). Only build this when a real workload needs a non-safetensors format — until then the single-format seam is the right call, not a premature abstraction. |
| Identify the NTM kernel pattern that triggers paravirt-GPU hangs (if it recurs) | bug | M | Diagnosed 2026-05-18: in the Tart VM, a single mlx-gpu workload at 19:44Z wedged the paravirt GPU (`AppleParavirtGPUMetalIOGPUFamily`), after which every subsequent `MTLComputePipelineState` creation failed (mlx: `Unable to load kernel … / Compilation failed`; PyTorch MPS: `CompilerError Code=2`) until the VM was rebooted. Diagnosis is in `docs/develop/gotchas.md` (MLX Backend → "Paravirt-GPU hang: every kernel fails until VM reboot"); recovery procedure lives in the `project_mlx_gpu_environment.md` memory file; perf scripts mark crashed cells correctly as of commit `7faca59`. Post-reboot all examples (including ntm-copy / ntm-recall mlx-gpu) ran cleanly, so the trigger appears rare rather than deterministic. Open question, low-priority unless it recurs: which kernel pattern triggered the watchdog? rnn / lstm / gru / transformer mlx-gpu sweeps ran cleanly before ntm-copy fired the hang, suggesting an NTM-specific shape — likely the cosine-similarity or softmax inner loop over the M memory rows. If it recurs, the lever is either chunking those kernels in `backend_mlx.cpp` or documenting as a paravirt limitation. Reprioritize on next occurrence. |
| Perf-envelope characterization (when does each backend win?) | research | M | Today `bench-compare` reports ratios for 6 examples (rnn/lstm/gru/transformer/ntm-copy/ntm-recall) all in one regime: small batch (≤32), short op chains, sub-ms kernels. In that regime Idris-ml wins on tape (0.10-0.35× PyTorch). But we can't extrapolate to "Idris-ml is faster in general" — perf is regime-dependent (batch size, dim, op fusion potential, recurrent depth). To get principled confidence: characterize the envelope. **Three sub-deliverables:** **(1) Per-op microbench shape sweep** — extend `bench-ops-compare` to time matmul / conv / softmax / linear / lstm_cell / gru_cell / attention / scaled_dot_product_attention at N ∈ {64, 256, 1024, 4096} on each backend + PyTorch. Output: 2D heatmap per op showing which backend wins per shape. Cheap (each cell is one short run) and tells us *exactly* where the per-op crossover lives. **(2) Batch-size sweep** — pick lstm + transformer, run at batch ∈ {1, 8, 32, 128, 512}. Today bench-compare is all batch ≈ 1-16 implicitly; the "Idris wins → PyTorch wins" crossover almost certainly lives in batch dimension. **(3) Large-workload datapoint** — pairs with the "LLM-class example (Llama-7B-shape inference)" row. LLM inference is where PyTorch+Inductor (or +MPS) is mature; if Idris-ml on mlx-gpu lands within ~2× of PyTorch+MPS on tiny-llama, that's a strong general claim. **Framing**: tape's value isn't to dominate PyTorch in production — it's to be the fastest way to run small examples that demonstrate idris-ml's *correctness* and *type-system value*. The envelope work establishes where tape (and the other backends) shine, so users pick the right one. **Deliverable**: `docs/develop/perf-envelope.md` with per-(workload regime, backend) cells documenting "Idris wins / PyTorch wins / tied" with a short mechanism note (dispatch vs kernel vs graph compile) per cell. Cross-references: `feedback_vm_perf_noise.md` (single-run noise ≥15-20% so use multi-run CIs); `MatmulBench.idr` already covers matmul N axis; the "Match PyTorch's catalogue of fused ops (epic)" row (kernel-level wins live there). |
| Broadcasting (typed user-facing) | feature | XL | Type-safe broadcasting at the user-facing tensor API. **Status**: internal C-side broadcasting machinery is in place — `binop_elementwise` handles 2D broadcast pairs, `reduce_grad` correctly accumulates broadcast backward shapes (commits in 2025 CHANGELOG), and scalar broadcasts ride through `tmulScalar` / `taddScalar` / etc. What's still missing is the **typed Idris-side API**: `tadd : Tensor [m,n] d dt g -> Tensor [n] d dt g -> Tensor [m,n] d dt g` (and ranks beyond) with a `Broadcastable` typeclass / type-level relation gating admissible pairs, so callers can write the same idiom they'd write in NumPy / PyTorch and the shape mismatch is rejected at elaboration. Key tension: expressiveness vs shape safety guarantees. Dex's typed index sets are the most promising model for a dependently-typed setting. The C-side machinery already exists; this row is just the type-level relation + Idris-side API. See `docs/static-vs-dynamic-graphs.md` |
| Reshaping layers | feature | M | No current use case |
| Double DQN / Dueling DQN | feature | S | Variants atop base DQN. Double DQN uses the online network to select actions and the target network to evaluate them (reduces maximization bias). Dueling DQN splits the Q-head into value + advantage streams. File once base DQN is stable |
| Prioritized experience replay / N-step returns / Rainbow | feature | M | Value-based enhancements atop DQN. Prioritized replay samples transitions proportional to TD-error; N-step returns bootstrap after N steps instead of 1; Rainbow combines PER + N-step + Double + Dueling + noisy nets + distributional. Worth it only if we want to push DQN toward SOTA on harder envs |
| Q-learning on Taxi and FrozenLake | feature | S | Same algorithm as the CliffWalking example, different envs. Low marginal algorithmic value but useful for Jupyter tutorials (FrozenLake stochastic slippery dynamics visualize well, Taxi exercises the 500-state table) |
| Value / Policy iteration (model-based tabular) | feature | M | Requires exposing transition probabilities from stochastic gym envs (FrozenLake, Blackjack). Gym-side change — add `transitions : state -> action -> List (Double, state)` to the Env interface, or a separate `ModelBasedEnv` interface. Then Bellman-optimality iteration is ~30 lines |
| Tabular exploration bandits (UCB, Thompson sampling) | feature | S | Multi-armed bandit algorithms. No current env motivates since toy-text envs are full MDPs, not bandits. Would need a `MultiArmedBandit` env (Gaussian or Bernoulli arms) in `Gym.ToyText` first |
| SAC on LunarLander-continuous | feature | S | Follow-up once Box2D envs land. SAC's existing continuous-control pathway should apply directly to LunarLander-continuous with minimal changes |
| Box2D gym envs | feature | L | LunarLander, BipedalWalker, CarRacing. Require a 2D rigid-body physics engine (contacts, joints, rotation). LunarLander is the simplest (~500 lines of rigid-body physics in pure Idris). BipedalWalker and CarRacing need articulated bodies and tile-based tracks respectively |
| Atari + MuJoCo gym envs | feature | XL | Atari needs ROM emulation (ALE equivalent); MuJoCo needs an MJCF-compatible physics engine. Out of scope for pure Idris short term |
| Idris 2 C backend (custom) | research | XL | If RefC doesn't work out: custom Idris 2→C code generator optimized for tensor workloads. Much higher effort than RefC investigation above |
| CodeMirror Idris 2 mode | feature | S–M | Jupyter kernel uses `"codemirror_mode": "haskell"` as a fallback. Investigate whether an Idris 2 CodeMirror grammar exists or could be written (CodeMirror 6 Lezer grammar). Would give proper syntax highlighting in JupyterLab |
| RefC backend adoption | research | S–M | Blocked on upstream Idris 2 RefC bugs. Our codebase is RefC-ready (zero Scheme FFI, Compat.Random, shims for missing runtime functions). RefC crashes in `idris2_trampoline` on nested ADTs (Tensor Functor map over Vect of STensor). Simple C FFI programs work. Revisit when Idris 2 releases post-0.8.0 with updated RefC runtime. See `docs/develop/refc-investigation.md` and `docs/develop/refc-upstream-bug.md` |
| Upstream Idris 2 RefC bug report | external | S | File issue on idris-lang/Idris2 for: RefC 0.8.0 trampoline crash on nested algebraic data types (Functor map over Vect of constructors). Repro, ASAN trace, and draft report in `docs/develop/refc-upstream-bug.md`. Also: (1) contrib System.Random needs `%foreign "C:..."` for RefC compat, (2) 0.8.0 runtime missing `idris2_negate_Double` etc. (fixed in main) |
| Unify `*_ARGS` convention for example targets | infra | S | Each example target declares its own override variable (`GPT_ARGS`, `MNIST_ARGS`, `DQN_ARGS`, ...). test-examples hardcodes the name-to-variable mapping in a `case` statement. If someone renames `DQN_ARGS` → `DQN_EXTRA_ARGS`, test-examples silently falls back to defaults — no error, just slow CI. Either standardise on one name (e.g. `EXTRA_ARGS`) across all example-* targets, or declare the mapping as Make variables adjacent to the targets so drift breaks loudly |
| Make build / coverage output less noisy (think about it properly) | infra | S | Standard make echoes every command verbatim. For a from-scratch backend build that's ~93 (torch) + 87 (mlx) + 109 (tape) per-cpp compile lines, a dylib link line that's >200 .o paths long, a `test_criterion_smoke` link line that lists ~40 .c sources, plus the `llvm-cov report` dump on coverage targets — much of the screen real estate per cold build. The decision today (2026-05-26) was to keep make-standard behavior — verbose IS the make convention, not a defect. The follow-up here is to think properly about whether to adopt kernel-style suppression (`@cmd` everywhere + `make V=1` escape hatch), which patterns matter to which audiences (CI logs vs interactive local builds vs the noisy `make coverage-backend-torch` specifically — that one being the most disproportionate-noise target), and whether a "coverage targets only" middle ground makes sense. Surfaces to touch: Makefile per-TU compile rules (lines ~349-365 for backend_tape/torch/mlx), the dylib link, the `test_criterion_smoke` recipe (~672), and the `coverage-backend` recipe + its `llvm-cov report` dump (~698-708). Don't reach for this until a real workflow tells you which mode wins — premature toggle hurts the people who currently rely on the standard output for debugging. |
| Investigate macOS CBLAS deprecation warnings | infra | S | `backend_tape.c`'s `cblas_dgemm`/`cblas_dgemv` calls (matmul/mv kernels) warn `'cblas_dgemm' is deprecated: first deprecated in macOS 13.3` under Apple's Accelerate. Apple's fix is to compile with `-DACCELERATE_NEW_LAPACK` (and `-DACCELERATE_LAPACK_ILP64` for 64-bit indices) to get the updated CBLAS interface. Currently just noise (the legacy symbols still link + work), but it'll break when Apple eventually removes them. Audit: add the `-DACCELERATE_NEW_LAPACK` define to the tape build's macOS CFLAGS, confirm the kernels still produce identical results (the ILP64 variant changes the int width — use the non-ILP64 new-LAPACK first), and check no other backend hits the same warning. Linux/OpenBLAS is unaffected. |
| Investigate `tape_load_d` per-element branch cost on F32 hot loops | research | M | The Phase 3 / 3b F32 routing on `backend_tape.c` reads input data through `tape_load_d(t, i)` — a one-line inline that branches on `t->dtype_tag` and returns `(double)((float*)t->data)[i]` or `((double*)t->data)[i]`. Used everywhere a backward case touches input data (~12 cases) and in F32 forward bodies for conv/pool/norm. The branch reads a single `int` field that the optimizer should hoist out of inner loops (the tensor doesn't change inside the loop), and at use sites with a single tensor in scope the cast is predictable. **But**: Phase 3 didn't profile. Investigate whether (a) the branch actually hoists in `-O2` builds of `backend_tape.c`, (b) the cost is measurable on a representative F32 workload (e.g. an F32 transformer-shape matmul chain, or a CNN forward+backward), and (c) if it dominates a hot loop, replace per-element dispatch with a one-shot type-cast at function entry: load the typed pointer once (`const float* a_f = is_f32 ? (const float*)a->data : NULL;`) and branch on `is_f32` at the outer loop level instead of per element. Pairs with the deferred backward-switch `.inc` extraction (currently out of scope per the modular-petting plan) — if the branch *does* dominate, the right fix may be to stamp the backward switch f64+f32 once. **Deliverable**: a perf-log entry from running an F32-heavy workload pre- and post-investigation, with either "no measurable cost — leave as is" or a specific kernel batch + the rewrite. Cross-references: `docs/develop/perf-log.jsonl`, `backend_tape.c` (`tape_load_d` definition + ~40 call sites in F32-routed kernels), the Phase 3b closeout (commit `c214c9b`). |
| Localize gpt gradient divergence: tape/mlx vs torch backend | bug | M | `make example-gpt GPT_ARGS="--epochs 3 --seed 99"` lands at bpc 5.2887 on tape, mlx-cpu, and mlx-gpu but 5.0840 on the torch backend (PyTorch ref ≈ 5.042). Measured 2026-05-21: epoch-0 training loss is bit-identical (4.334873) so forward + init match exactly across backends; the gap opens only through the optimizer steps and grows over the 3 steps. Ruled out as the cause: (1) the optimizer — AdamW's per-element update `lr·mhat/(√vhat+ε)` is algebraically identical to torch.optim.AdamW including eps placement, and decoupled weight-decay ordering contributes only ~1e-7/param/step at gpt's `lr·wd = 1e-4`; (2) grad-norm clip — `clip_grad_norm_opt` matches PyTorch's `scale = max_norm/norm` form. The remaining suspect is **gradient-level**: tape (hand-written C reverse-mode backward) and mlx (mlx autodiff vjp) are independent engines yet agree to 6 sig figs, with libtorch as the lone outlier — pointing at a per-op backward where the torch backend dispatches a fused libtorch kernel (softmax / layernorm / scaled-dot-product attention / embedding / cross-entropy) whose gradient differs subtly from the tape/mlx primitive decomposition (forward agrees, backward doesn't). To localize: dump per-layer gradient norms after one backward on a fixed seed for tape vs torch (e.g. via a gradient analogue of `forwardVarTraced`) and find the first layer whose grad diverges. Low priority because tape/mlx are mutually self-consistent and the discrepancy is a short-horizon smoke-test artifact, not a known convergence failure — but worth resolving since one side is wrong vs PyTorch. Earlier mis-diagnosed as RMSprop lr-folding (gpt uses AdamW, not RMSprop); that fix landed separately as a genuine RMSprop alignment (commit `90bca2c`). **2026-05-26 update**: a minimal Criterion test (`test_clip_grad_norm.c`, W3b coverage) reproduces the same shape: tape passes at 1e-12, mlx passes at 1e-4 (its native F32 storage), torch shows ~9e-8 drift in the post-clip gradient values even with F64 params/grads. The drift is ~1 F32 ULP at the test magnitudes, so the torch-side divergence is showing up as if some intermediate accumulator runs in F32. Sentinel: the new test asserts at 1e-6 — tighten when this row closes. |
| Consider rewriting tape backend in C++ | research | M | After the modularization lands tape will be the only C-language backend (torch + mlx are C++). Symmetric language across all three backends would unlock: (1) sharing the C++ adapter machinery without `extern "C"` shims at internal boundaries, (2) RAII for tape lifetime / arena scopes / param registry (replacing the manual retain/release dance in adapter callbacks), (3) `std::variant` for the per-op meta payloads (currently `void*` with manual cast switches in backward), (4) templates for the F32/F64 stamped forward kernels (eliminating the two `.inc` includes), (5) easier integration with Criterion's C++-friendly assertions. Real cost: rewriting ~7K lines of working numerical code is risky regardless of language, the FFI surface (Idris ↔ tape) must stay `extern "C"` either way, and the current C build has zero non-libc deps (no libstdc++ dependency on the tape lane). Only worth doing if the modularization shakes out adapter friction that C++ would clean up — file as a follow-up consideration not a commitment, with a decision triggered by concrete pain post-modularization, not language preference. |
| Paired-side architecture drift checker | infra | M | Companion to the hyperparameter drift checker (`scripts/check-paired-defaults.py`) which only sees `defaultConfig` / argparse defaults. This one would catch drift in **model architecture** between `Example/*.idr` and `torch_ref/{models,scripts}/<x>.py`: layer widths, hidden sizes, optimizer choice, init scheme, loss function, hardcoded reward-shaping constants. Two sub-problems, do (A) first: **(A) Named-constant drift** — both sides typically declare arch constants as named bindings (Idris top-level: `N : Nat\nN = 128`, `M = 20`, `H = 100`; Python dataclass: `n: int = 128`, `m: int = 20`, `controller_size: int = 100`). Cleanly extractable with regex + AST. Mapping table per-example: `{"N": "n", "M": "m", "H": "controller_size"}`. Catches the high-stakes drift class (NTM/DNC dimension changes, GPT vocab/seq-len, controller widths). **(B) Layer-graph drift** — harder. Idris-side encodes architecture as a model expression like `linearLayerAny {i=4} {o=128} ~~> tanhLayerAny ~~> OutputLayer linearLayerAny {i=128} {o=2}`; PyTorch-side encodes it in `nn.Module.__init__` + `forward`. Needs an AST normalisation step on both sides producing a comparable layer graph (a flat list of `(LayerKind, args)` tuples), then a diff. Idris-side parsing benefits from the `~~>` / `OutputLayer` shape being structured; Python-side needs walking `__init__` for `nn.X(...)` assignments plus the `forward` method for the operation order. The `forwardVarTraced` infrastructure on the Idris side already produces a layer-by-layer trace at runtime — might be a cheaper path than static parsing if we accept "run both sides, compare traces". Out-of-scope intentional divergences (paired-side init differences documented in `reference-alignment.md`; mlx-only f32 cast etc.) need per-example overrides like the hyperparameter checker has. Deliverable: `scripts/check-paired-arch.py` + `make check-paired-arch` + CI gate (after deduping with `check-paired-defaults`). Saves the same class of mid-PR confusion the hyperparameter checker saves, one architecture-shaped layer up |
| Rewrite the backend code in Rust for memory safety | research | L–XL | Replace the current C / C++ backend implementations (`packages/backends/backend_tape/` ~7K LOC C; `backend_torch/` C++ around libtorch; `backend_mlx/` C++ around mlx) with a Rust implementation, exposing the same `extern "C"` FFI surface to Idris. **Motivation**: memory-safety class bugs already cost real engineering time on this codebase — the `transformer mlx-gpu intermittent crash` Low-priority row above is an unresolved stale `shared_ptr<ArrayDesc>` problem detected via crash-trace at a stable freed-memory poison address; the chunked-arena fix in `feedback_no_rebuild_during_running_harness` memory was needed because the prior `realloc`-based arena invalidated all outstanding pointers; the wrapped-handle ABI (`docs/develop/tensor-lifecycle.md`) exists because Chez Scheme vectors silently lose foreign pointers under GC. Rust's ownership model rules out the entire **use-after-free / dangling-shared_ptr / NULL-deref / buffer-overrun** bug classes at compile time. The borrow checker + lifetimes encode tensor / arena / handle relationships the C code currently maintains by convention. **What gets gained beyond memory safety**: (1) `cargo` replaces the hand-rolled multi-link Makefile machinery (each lane's `BACKEND=` config becomes a Cargo feature flag); (2) per-op meta payloads (currently `void*` + manual cast switches in tape backward) become `enum` variants the compiler proves exhaustive; (3) Criterion-style testing replaces the C-side Criterion + per-`#ifdef BACKEND_*` blocks (Rust's `#[cfg]` is cleaner per-backend gating); (4) the tape arena's per-chunk lifetime fits naturally into Rust's typed-arena pattern (`bumpalo` / `typed-arena`) instead of the current manual refcounting; (5) easier-to-reason-about FFI safety — `unsafe` blocks are explicit + auditable, vs C's implicit unsafety everywhere. **What's hard**: (a) **the libtorch + mlx bindings problem** — libtorch has `tch-rs` (mature but C++ FFI is fragile; ABI changes require crate updates); mlx has no mature Rust binding (`mlx-rs` is experimental and lags upstream). Either we vendor binding generation (`bindgen` + manual hand-fixes for the C++ classes) or stay on a C++ shim for the libtorch/mlx surface and only Rust-ize the tape backend + the cross-cutting safetensors / param-registry / lifecycle layers. The "Rust-ize tape only" path is a more tractable first step. (b) **Rewriting ~7K LOC of working numerical code is risky regardless of language** — every backward op needs paired-side verification; the existing `make test-backend-criterion-{tape,torch,mlx}` 204/204 / 194/194 / 196/196 assertions are the gate. (c) **Idris ↔ Rust FFI is unusual** — works fine through a `extern "C"` boundary but the wrapped-handle ABI (Chez vector slot 1 = backend tag, slot 2 = raw pointer) needs to be preserved bit-exactly; the lifecycle scripts under `scripts/lifecycle/` would need updating. (d) **The build system gets bilingual** — Cargo + Make; cleaner to introspect via Cargo workspace but messier in the Makefile. (e) **No external `unsafe`-policing** — Rust's safety is only as strong as the developer-written `unsafe` blocks at the FFI boundary; we'd still need manual review there. **Cheaper alternatives that should land first** (and may obviate this row): (1) wire up `clang-tidy` / `cppcheck` per the existing Low-priority row above — catches a meaningful subset of memory-safety bugs at near-zero cost; (2) run the C tests under AddressSanitizer + UndefinedBehaviorSanitizer per CI — same machinery PyTorch uses; (3) the C++ rewrite of the tape backend (existing Low-priority row above, line 77) — gives RAII + `std::variant` for op-meta without abandoning the C++ ecosystem the torch/mlx backends already depend on. **Decision trigger**: this row is worth picking up if (i) a memory-safety bug in the C code costs a multi-day debug cycle that ASAN didn't catch, OR (ii) Rust's mlx binding becomes mature enough to consider production use. Until then it's a research direction, not a commitment. **Approach if it lands**: start with a non-numerical surface (the param registry or the safetensors loader) — these are pure data plumbing, easy to verify against the existing C versions byte-for-byte. Build out from there. Cross-references: existing rows "Consider rewriting tape backend in C++" (line 77, the conservative-language alternative), "Wire up `clang-tidy` / `cppcheck` for the C backends" (line 76, the cheaper-first-step alternative), "transformer mlx-gpu intermittent crash" (line 51, the motivating example bug class). |
| Bound resource usage (memory + handle count) | feature | M | Two unbounded resources today, both observable but neither capped: **(1) Memory** — tape-backend arena (the chunked allocator in `backend_tape/arena.c`) grows linearly with example complexity and epoch count; torch carries an `intermediates` vector; mlx's MTLBuffer pool grows in lockstep. A long-running training can OOM with no upper bound. Observability: add `prim__memUsed` / `prim__memPeak` and surface them in `runTraining`'s logEpoch line alongside RSS (RSS is already there). Enforcement: a `--memory-limit` CLI flag (or env `IDRIS_ML_MEM_LIMIT`) that aborts the run with a clear error when arena + persistent param footprint exceeds the limit, instead of letting the OS kill the process. Tape's chunked arena makes the check cheap — each new chunk request can compare the running total. Likely also wire to mlx's `mx::set_memory_limit`. **(2) Handle count** — live Tensor handle counts (`tensor_live_count` / `tensor_peak_live_count`, already surfaced in the logEpoch `handles=` field and the timing-summary `Live handles` / `Peak handles` lines) also grow without bound when something leaks. Observability is done; enforcement isn't — a `--handle-limit` flag (or env) that aborts on threshold-cross catches the bug class "forgot to free per-sequence state tensor" instantly, before the run lopes for hours and dies. Same shape as the memory cap: cheap counter, abort path, surfaced in the per-epoch line. Useful both for CI hygiene and for catching unintentional state leaks. Cross-references: `backend_tape/arena.c` (chunked allocator), `tensor_live_count` (lifecycle counter), `docs/develop/tensor-lifecycle.md`. |

**Explicitly not planned:** distributed training (infrastructure, not library), mixed precision/quantization (performance optimisation), model zoo (compositions of existing primitives), TorchScript (our type system is the compile-time analysis), bidirectional RNN (transformers have obsoleted), exotic losses (compose from primitives)

Completed work has moved to [CHANGELOG.md](CHANGELOG.md).
